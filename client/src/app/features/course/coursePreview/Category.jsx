function Category({ category }) {
  return (
    <div className="inline-block bg-primary/10 text-primary px-4 py-2 rounded-full text-sm uppercase tracking-wide">
      {category}
    </div>
  );
}

export default Category;
